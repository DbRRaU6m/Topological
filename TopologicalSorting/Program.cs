using GraphAlgorithms;
using Graphs;
using Serialization;
using System.Linq;

namespace TopologicalSorting
{
    class Program
    {
        static void Main(string[] args)
        {
            // no real functionality, the implemented algorithm
            // is presented via unit tests

            /*
            Serializer.SerializeToXml<Graph>(@"Z:\Graph-01.xml", iGraph);
            */
        }
    }
}
